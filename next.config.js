const { domainToASCII } = require('url')

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {Domains:["media.istockphoto.com"],
}

//module.exports = nextConfig
}
